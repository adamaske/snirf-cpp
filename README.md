# snirf-cpp

# Installation
git clone --recurse https://github.com/adamaske/snirf-cpp.git

In your CMakeLists

add_subdirectory("path/to/snirf-cpp)

target_link_libraries(your-target PRIVATE/PUBLIC snirf)

# Usage
Example usage to load a snirf file and print its contents

Example of retreiving the events. etc. 



